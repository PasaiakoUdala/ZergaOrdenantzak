{% extends 'raw.html.twig' %}

{% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('default-list') }}
{% endblock %}

{% block menua %}
	<nav id="logo_lerroa" class="navbar navbar-fixed-top">
		<div class="container">
			<div class="navbar-header">
				<img id="logoa" src="{{ asset('build/images/logo.png') }}" alt="Amorebieta-Etxanoko Udala"/>
			</div>
			<span id="itzuli_esteka" class="navbar-right">
				<a href="https://www.amorebieta-etxano.eus/">{{ "Volver a la web del ayuntamiento" | trans }}</a>
			</span>
		</div>
	</nav>
	<nav id="menu_lerroa" class="navbar navbar-default">
		<div class="container-fluid">
			<div class="navbar-header">
				<a class="navbar-brand"></a>
				<ul class="nav navbar-nav">
					<li class="list-inline">
						<a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({'_locale': 'es'})) }}">ES</a>
					</li>
					<li class="list-inline">
						<a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({'_locale': 'eu'})) }}">EU</a>
					</li>
				</ul>
			</div>
			<div id="navbar" class="navbar-collapse collapse navbar-right"></div>
		</div>
	</nav>
{% endblock %}

{% block body %}
	<div class="modal fade" id="nireModal" role="dialog" tabindex="-1">
		<div class="modal-dialog modal-lg" role="document">
			<div class="modal-content">
				<div class="modal-header">
					<button aria-label="Close" class="close" data-dismiss="modal" type="button">
						<span aria-hidden="true">×</span>
					</button>

					<h4 class="modal-title" id="modalTitle">&nbsp;</h4>
				</div>

				<div class="modal-body" id="modalContent">&nbsp;</div>

				<div class="modal-footer">
					<button class="btn btn-primary" data-dismiss="modal" type="button">Cerrar</button>
				</div>
			</div>
		</div>
		<!-- /.modal-content -->
	</div>
	<!-- /.modal-dialog -->
	<!-- /.modal -->
	<div class="container-fluid alert alert-info" role="alert">{{ "La información ofrecida a continuación carece de validez jurídica y se ofrece sólo a efectos informativos" | trans }}</div>

	<div class="container alert alert-warning">
		{% if app.request.locale == 'eu' %}
			{% set url = 'https://amorebieta-etxano.eus/wp-content/uploads/2025/01/Egutegi_fiskala.jpg' %}
		{% else %}
			{% set url = 'https://amorebieta-etxano.eus/wp-content/uploads/2025/01/Calendario_fiscal.jpg' %}
		{% endif %}
		<a href="{{ url }}" target="_blank">{{ "Calendario Fiscal" | trans | upper }}</a>
	</div>

	<ul id="ulzzoo"></ul>
	<div id="app" data-api-url="{{ apiUrl }}">
</div>	
{% endblock %}

{% block javascripts %}
	{{ parent()}}
	{{ encore_entry_script_tags('default-list') }}
{% endblock %}
